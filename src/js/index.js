import * as React from 'react';
import * as ReactDOM from 'react-dom';
import Application from './components/Application.jsx';

ReactDOM.render(React.createElement(Application, {}), window.document.getElementById('app'));