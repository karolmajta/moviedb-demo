import * as React from 'react';

export default function SearchInput (props, context) {
    return (
        <div className="SearchInput">
            This is a stub container for SearchInput.
        </div>
    );
}